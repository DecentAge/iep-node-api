{
  "name": "iep-node-api",
  "version": "0.3.3",
  "description": "Examples of XIN API calls base path is /api",
  "main": "index.js",
  "scripts": {
    "update-version": "npm version $npm_config_release_version --allow-same-version --git-tag-version false",
    "prestart": "npm install",
    "start": "node index.js"
  },
  "keywords": [
    "swagger"
  ],
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "swagger-tools": "0.10.1",
    "envsub": "4.0.7"
  }
}
